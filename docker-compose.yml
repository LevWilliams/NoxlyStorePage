services:
  web:
    build: .
    expose: ["8000"]
    volumes:
      - ./app:/app/app

  nginx:
    image: nginx:1.27-alpine
    depends_on: [web]
    ports: ["80:80"]
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
